# Simple working Temporal solution
# This creates a minimal Temporal frontend pod that can connect to PostgreSQL
# in the same namespace (data-plane)

apiVersion: v1
kind: ConfigMap
metadata:
  name: temporal-config
  namespace: data-plane
  labels:
    plane: data
    tenant: temporal-tenant
data:
  docker.yaml: |
    # Minimal Temporal configuration
    persistence:
      default:
        sql:
          pluginName: "postgres12"
          databaseName: "temporal"
          connectAddr: "postgresql-primary:5432"
          user: "temporal"
          password: "temporal"
      visibility:
        sql:
          pluginName: "postgres12"
          databaseName: "temporal_visibility"
          connectAddr: "postgresql-primary:5432"
          user: "temporal"
          password: "temporal"
    services:
      - "frontend"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: temporal-working
  namespace: data-plane
  labels:
    app: temporal-working
    plane: data
    tenant: temporal-tenant
spec:
  replicas: 1
  selector:
    matchLabels:
      app: temporal-working
  template:
    metadata:
      labels:
        app: temporal-working
        plane: data
        tenant: temporal-tenant
    spec:
      # Mount config as volume instead of writing to filesystem
      volumes:
      - name: config
        configMap:
          name: temporal-config
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: temporal
        image: temporalio/server:1.30.4
        # Use the config file from configMap
        command: ["/bin/bash"]
        args: 
        - "-c"
        - |
          # Copy config from volume (read-only) to writable location
          cp /etc/temporal-config/docker.yaml /tmp/docker.yaml
          # Start Temporal with config file
          temporal-server --env docker start --config /tmp/docker.yaml
        volumeMounts:
        - name: config
          mountPath: /etc/temporal-config
          readOnly: true
        securityContext:
          readOnlyRootFilesystem: true  # Container filesystem is read-only
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          seccompProfile:
            type: RuntimeDefault
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 200m
            memory: 512Mi
        ports:
        - containerPort: 7233
          name: frontend
        livenessProbe:
          httpGet:
            path: /health
            port: 7233
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 7233
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: temporal-working
  namespace: data-plane
  labels:
    app: temporal-working
    plane: data
spec:
  ports:
  - port: 7233
    targetPort: 7233
    name: frontend
  selector:
    app: temporal-working