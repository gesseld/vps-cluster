apiVersion: v1
kind: ResourceQuota
metadata:
  name: observability-plane-quota
  namespace: observability-plane
spec:
  hard:
    limits.cpu: "3"  # Increased from 1900m to 3000m to accommodate vmalert
    limits.memory: "3Gi"
    pods: "10"
    requests.cpu: "1"
    requests.memory: "1.5Gi"
